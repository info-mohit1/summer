# Summer Shop — Local Setup

## Run locally


## Deploy to Vercel

1. Push this repo to GitHub
2. Import on vercel.com → New Project
3. Set **Root Directory** to 
4. Add Environment Variable:
   -  = 
5. Click Deploy
6. In Vercel project **Settings → Deployment Protection** → disable Vercel Authentication

The  inside  handles the build command and SPA routing automatically.