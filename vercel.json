{
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/summer-shop run build",
  "outputDirectory": "artifacts/summer-shop/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
